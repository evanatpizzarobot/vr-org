services:
  vrorg:
    build: .
    container_name: vr-org
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
    mem_limit: 512m
